# Client-assisted validation

This feature is operator-only and optional.

- It must never generate canonical predictions.
- It may run deterministic metric aggregation and review candidate generation.
- The server must verify any accepted result.
- The feature stays disabled by default in all environments.
