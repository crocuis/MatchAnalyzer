{
  "name": "match-analyzer",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:web": "npm --workspace apps/web run test",
    "test:api": "npm --workspace apps/api run test"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
